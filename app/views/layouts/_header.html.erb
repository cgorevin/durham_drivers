<header>
  <%= link_to 'Second Chance Driving', root_path(locale: params[:locale]), class: 'logo' %>

  <input type="checkbox" class="nav-toggle" id="nav-toggle"/>

  <nav>
    <ul style="list-style: none;">
      <li><%= link_to t('.search'), root_path(locale: params[:locale]), class: 'nav-link' %></li>

      <% if I18n.locale == :en %>
        <li><%= link_to 'Español', root_path(locale: 'es'), class: 'nav-link' %></li>
      <% elsif I18n.locale == :es %>
        <li><%= link_to 'English', root_path(locale: 'en'), class: 'nav-link' %></li>
      <% end %>

      <% if admin_signed_in? %>
        <li><%= link_to 'Admin', panel_path, class: 'nav-link' %></li>
        <li><%= link_to 'Logout', destroy_admin_session_path, method: :delete, class: 'nav-link' %></li>
      <% end %>
    </ul>
  </nav>

  <label for="nav-toggle" class="nav-toggle-label">
    <span></span>
  </label>
</header>
