<%= form_for Contact.new do |f| %>
  <%= hidden_field_tag :ids, nil, value: params[:ids] %>

  <div class="form-row">
    <div class="form-group">
      <%= f.label :method, t('.contact_method') %>
      <%= f.radio_button :method, 'email', required: true %>
      <%= f.label :method_email, t('.email')%>
      <%= f.radio_button :method, 'phone', required: true %>
      <%= f.label :method_phone, t('.text') %>
    </div>

    <div class="form-group">
      <%= f.text_field :info, placeholder: t('.contact_info'), class: 'form-control'%>
    </div>
  </div>

  <div>
    <p><%= t ".selector" %></p>

    <p>
      <%= f.radio_button :requestor_name, ' ', class: 'js-radio', required: true %>
      <%= f.label :requestor_name__, t('.email_request') + "#{@offense.name}" + t('.email_request_end') %>
    </p>

    <p>
      <%= f.radio_button :requestor_name, true, class: 'js-radio', required: true %>
      <%= f.label :requestor_name_true,  t('.request_start') + "#{@offense.name}" + t('.request_end') %>
    </p>
    <div class="js-div">
      <%= t('.requestor_name') %>
      <%= f.text_field :requestor_name, class: 'js-field', disabled: true, required: true, placeholder: (t '.enter_name') %>
    </div>
  </div>

  <%= submit_tag t('.save_button'), class:'btn btn-primary' %>
<% end %>
