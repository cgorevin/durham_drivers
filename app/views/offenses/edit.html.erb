<h1>Edit Case</h1>

<p>
  This page will detail individual cases where you can edit status from pending to approved/rejected
  on a case-by-case basis.
</p>

<%= form_for @offense do |f| %>
  <%= f.label :status %>
  <%= f.select :status, ['pending', 'approved', 'denied'] %>

  <%= f.label :group %>
  <%= f.select :group, (1..52).to_a.unshift('NA') %>

  <div>
    <%= f.submit @offense.contacts.any? ? 'Save and send emails' : 'Save' %>
  </div>
<% end %>
